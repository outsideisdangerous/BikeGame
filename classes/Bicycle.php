<?php

class Bicycle {

  // Properties
  private $location;
  private $orientation; // N, S, E, W

  // Methods
  public function __construct($location, $orientation) {
    $this->location = $location;
    $this->orientation = $orientation;
  }

  function setInitLocation($x, $y) {
    echo  
  }

  public function move() {
   
  }
  
  detectEdge()
  ...
  
  rotate(degree = [-360, 360])
}

$newBike = new Bicycle();
$newBike->setInitLocation(userInputX, userInputY);

placeBike(0,0);

?>